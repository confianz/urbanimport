<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data>
        <record id="sale_order_braintree_form_view" model="ir.ui.view">
            <field name="name">Sale Order</field>
            <field name="model">sale.order</field>
            <field name="inherit_id" ref="sale.view_order_form"/>
            <field name="arch" type="xml">
                <xpath expr="//button[@name='action_quotation_send']" position="after">
                    <button name="create_and_open_payment_sale" id="sale_braintree_payment_btn" type="object"
                            class="oe_highlight"
                            attrs="{'invisible': [('state', '=', 'cancel')]}"
                            string="Open Braintree Payment" groups="account.group_account_invoice"/>
                </xpath>

            </field>
        </record>

    </data>
</odoo>